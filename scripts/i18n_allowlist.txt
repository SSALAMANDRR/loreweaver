# Whole-file exemptions for sanctioned game data / parser data modules.
agent/kp_tools_mechanics.py
agent/kp_tools_knowledge.py
agent/module_initializer.py
core/character_manager.py
core/game_clock.py
core/prompt_sections.py
core/rulepacks.py
core/skills.py

# Developer-facing advancement/creation validators. Their diagnostics are
# author/programmer errors, never player-facing UI; command layers localize
# user-visible failures before returning them.
core/advancement.py
core/advancement_purchase.py
core/advancement_surface.py
core/creation_flow.py
core/creation_layers.py
core/creation_profiles.py
core/sheets.py
core/starting_equipment.py
core/talent_advancement.py
core/talent_requirements.py

# Narrow runner exemptions for bot-on parser tokens.
gateway/runner.py::开启
gateway/runner.py::啟用

# Developer-facing ValueError diagnostics in the binary character-card parser; these are never shown
# raw to players (the importing tool layer wraps str(exc) in a localized template), matching the
# convention the sibling parser modules above are whole-file-exempt for.
core/charcard.py:: PNG does not contain a SillyTavern character card
core/charcard.py:: embedded character card payload is not a JSON object
core/charcard.py:: character card JSON must be an object

# Developer-facing PackError/PackRefError diagnostics in the .lwpack build/install layer; these are
# never shown raw to players — app.py wraps str(exc) in localized pack.* templates
# (locales/{en,zh}/pack.json), matching the parser-module convention above.
core/pack.py
infra/pack_source.py

# Developer-facing ValueError diagnostics in the panels.yaml schema validator (M15); raised into
# core/pack.py's PackError wrapping (author-time build/install errors), same convention as above.
core/panels.py

# Same convention for the presentation.yaml (演出资料包) schema validator (M19): author-time
# diagnostics, wrapped by core/pack.py into localized PackError text before any human sees them.
core/presentation.py

# The Scribe's NPC boundary-watch prompt block (_NPC_WATCH_BLOCK): model-facing instruction
# text, the same nature as the module's main _PROMPT (which the regex-shape heuristic happens
# to exempt on its own); it is never shown to a user. The whisper the lane produces IS
# user-facing and goes through locales/*/scribe.json like every other whisper.
agent/scribe.py::These lists mark SECRET boundaries, not complete minds